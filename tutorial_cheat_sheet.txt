1. Extract Repetitive Windowing Logic into a Function
Refactor the inline loops into a reusable create_windows() function

2. Rename Functions and Variables for Clarity
e.g., split() → time_split(), prep() → preprocess_data()

2. Add Constants at the Top - config.py
e.g., WINDOW_SIZE, STEP_AHEAD, TARGET_COL instead of hardcoded values

3. Use Type Hints
Add type annotations to all function definitions

4. Add Docstrings to Functions
Describe what each function does, its inputs/outputs

5.Avoid Abbreviated Variable Names
Replace tr, va, te with descriptive names

6. Use Section Headers to Separate Logical Blocks
e.g., # -------------------- Windowing --------------------

7. PEP8 Formatting
Ensure proper indentation, spacing, and line lengths

8. Extract Tensor Conversion to a Function (Optional)
to_tensor(X, y) to reduce duplication

9. Add Comments to Explain Non-Obvious Steps
Especially around indexing logic in window creation
